// React
import React, {
  useState,
  useMemo,
  useRef,
  useEffect,
  useCallback,
} from "react";

import { useSelector, useDispatch } from "react-redux";

import { Stack } from "@mui/material";

// components
import GridHeader from "../gridHeader/gridHeader";
import MainGrid from "../mainGrid/mainGrid";
import TranspositionGrid from "../tranpositionGrid/transpositionGrid";

import * as ghf from "../gridHeader/gridHeaderFuncs";

export default function GridView2() {
  return (

    
  
  );
}
